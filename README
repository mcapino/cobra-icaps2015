Decentralized Multi-robot Planning
==================================

Compiling
---------
The dependencies are managed uding Maven. To compile the project run:

$ mvn package

The compiled project together with all dependencies will be in 
	target/admap-1.0-SNAPSHOT-jar-with-dependencies.jar

Alternatively, you can open the project in your favorite IDE, i.e. in Eclipse.

Getting started
--------------------

The planners are run through cz.agents.admap.creator.ScenarioCreator class. E.g. 

java -jar target/admap-1.0-SNAPSHOT-jar-with-dependencies.jar cz.agents.admap.creator.ScenarioCreator -problemfile instances/willow-r30-docks/56.xml -maxtime 15000 -timestep 45 -summary -showvis -bgimg /home/capino/projects/deconflictiontools/src/main/resources/environments/willow-darker.png


